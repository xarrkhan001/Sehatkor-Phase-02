# Local development overrides for the client
# Point the frontend to your locally running backend so new routes are available
VITE_API_BASE_URL=http://localhost:4000
VITE_SOCKET_URL=http://localhost:4000
